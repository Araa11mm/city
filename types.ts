
export interface MotionConfig {
  isQuietMode: boolean;
  prefersReducedMotion: boolean;
}

export enum SectionId {
  Hero = 'hero',
  Metro = 'metro',
  Noise = 'noise',
  NoOff = 'no-off',
  Final = 'final'
}
